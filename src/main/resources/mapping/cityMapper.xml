<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace：命名空间，用于隔离sql，用于动态代理 -->
<mapper namespace="smg.Vsoapmac.mapper.cityMapper">
	<select id="findAll" resultType="smg.Vsoapmac.bean.city">
		select * from city;
	</select>
</mapper>